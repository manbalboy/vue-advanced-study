export default  {
    SET_NEWS(state, oData) {
        state.news = oData;
    },

    SET_ASK (state, oData) {
        state.ask = oData;
    },

    SET_JOBS (state, oData) {
        state.jobs = oData
    }

}